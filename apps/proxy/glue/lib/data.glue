(module lib.data
    (export select-from select select-bind title)

    (def (select-from source go key)
        (listen (lookup source key) go)
    )

    (def select
        (select-from store.persistent)
    )

    (def (title props)
        (?? props.title props.code)
    )
)
