(module lib.navigation
    (export back go)

    (def back pop)
 
    (def (go route)
        (\ () (push
                :route route.route
                :id route.id
                :theme current-theme
            )
        )
    )
)
