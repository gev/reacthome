(module screen.site
    (export site-screen)

    (import lib.data)
    (import widget.screen)
    (import widget.site)

    (def (site-screen args)
        (screen
            :body (select site args.id)
        )
    )
)
