(module screen.site
    (export site-screen)

    (import widget.site)
    (import lib.vision)

    (def (site-screen props)
        (screen
            :body (select-persistent site args.id)
        )
    )
)
