(module screen.curtain
    (export curtain-screen)

    (import lib.data)
    (import widget.screen)
    (import widget.curtain)

    (def (curtain-screen args)
        (view
            (\ (props)
                (view (curtain props) props.bind)
            ) args.id
        )
    )
)
