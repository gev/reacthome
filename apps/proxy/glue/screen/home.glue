(module screen.home
    (export home-screen)

    (import screen.daemon)
    (import screen.site)

    (def (daemon-route home args)
        (route
            :screen (daemon-screen :id home.proxy)
        )
    )

    (def (site-route args)
        (route
            :screen (site-screen args)
        )
    )

    (def (home-screen home args)
        (navigator
            :routes (
                :index (daemon-route home)
                :site site-route
            )
        )
    )

)
