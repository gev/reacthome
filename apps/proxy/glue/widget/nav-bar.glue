(module widget.nav-bar
    (export nav-bar)

    (import locale.nav-bar)
    (import widget.floating-bar)

    (def (nav-bar props)
        (floating-bar
            :child (navigation-bar
                :selected-index 1
                :background-color colors.transparent
                :destinations (
                    (navigation-destination
                        :icon (icon :icon (symbol 'weather.mix))
                        :label (@ weather' locale.language-code)
                    )
                    (navigation-destination
                        :icon (icon :icon (symbol 'home) :fill 1)
                        :label (@ home' locale.language-code)
                    )
                    (navigation-destination
                        :icon (icon :icon (symbol 'dashboard))
                        :label (@ systems' locale.language-code)
                    )
                    (navigation-destination
                        :icon (icon :icon (symbol 'settings))
                        :label (@ settings' locale.language-code)
                    )
                )
            )
        )
    )
)
