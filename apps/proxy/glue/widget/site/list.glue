(module widget.site.list
    (export site-list)

    (import lib.vision)
    (import widget.site.card)

    (def (site-list sites)
        (column
            :spacing 16
            :children (map (select-persistent site-card) sites)
        )
    )
)
