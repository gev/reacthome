(module widget.site.list
    (export site-list)

    (import lib.data)
    (import widget.site.card)

    (def (site-list sites)
        (column
            :spacing 16
            :children (map (select site-card) sites)
        )
    )
)
