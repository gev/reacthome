(module screen.home
    (export home-screen)

    (import lib.vision)

    (def (home-screen props)
        (screen
            :body (center
                :child (
                    column
                    :main-axis-size main-axis-size.min
                    :children (
                        (image
                            :src "icon.png"
                            :width 100)
                        (sized-box :height 40)
                        (text :content props.title)
                        (listen
                            (lookup store.persistent 'data.counter 0)
                            (\ (counter) (text :content counter))
                        )
                        (text :content (foo 100))
                        (sized-box :height 40)
                        (row
                            :main-axis-size main-axis-size.min
                            :children (
                                (filled-button
                                    :child (text :content "Вперед >")
                                    :on-pressed (go 'next))
                            )
                        )
                    )
                )
            )
        )
    )
)
