(module lib.vision
    (export go-back go-next)

    (def go-back
        (\ () (pop ()))
    )

    (def go-next
        (\ () (
                (import screen.next)
                (push (route next))
            ))
    )
)
