(module lib.vision (export go)
    (def (go key)
        (\ () (push
                (route
                    (listen
                        (lookup store.tmp key (screen placeholder))
                        (\ (next) next)
                    )
                )
            )
        )
    )
)
